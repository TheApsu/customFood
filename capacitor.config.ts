import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.customfood.app',
  appName: 'customFood',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
